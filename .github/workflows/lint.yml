name: Lint

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.OS }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.OS }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Run linters
        uses: wearerequired/lint-action@v2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          eslint: true
          prettier: true
          auto_fix: true
          eslint_extensions: js,jsx,ts,tsx
          prettier_extensions: js,jsx,ts,tsx,css,md,yml,yaml,json
          commit_message: "style: fix code style issues with Prettier"

      - name: Check for trailing whitespace and newlines
        run: |
          ! grep -rn '[[:blank:]]$' --exclude-dir={.git,node_modules} .
          ! find . -type f -not -path '*/\.git/*' -not -path '*/node_modules/*' -print0 | xargs -0 -L1 bash -c 'test "$(tail -c 1 "$0")" && echo "No newline at end of $0"'
